{
  "name": "invooblast-smtp-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Relais HTTP→SMTP open source pour INVOOBLAST (Gmail App Password). Local ou déployé (Docker, Render…).",
  "engines": {
    "node": "22.x"
  },
  "type": "module",
  "main": "smtp-relay.mjs",
  "scripts": {
    "start": "node smtp-relay.mjs"
  },
  "dependencies": {
    "imapflow": "^1.0.186",
    "nodemailer": "^6.9.16"
  }
}
